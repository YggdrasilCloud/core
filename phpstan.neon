includes:
    - phpstan-baseline.neon

parameters:
    level: 8
    paths:
        - src
        - tests

    # Ignore deprecated warnings for now
    reportUnmatchedIgnoredErrors: false

    # Exclude patterns
    excludePaths:
        - tests/bootstrap.php
        - var/*
        - scripts/*
